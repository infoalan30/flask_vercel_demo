{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)", // Matches /api/get_age
      "dest": "api/index.py" // Sends it to your Flask app
    }
  ]
}
